# Branch Notes

This repository currently uses a simplified Git workflow:

- `main` is the only active local and remote branch.
- Feature branches are not used by default.
- Create a feature branch only when explicitly requested.

Before pushing, verify that the active branch is `main`.
