# Cost Inventory

Azure cost optimization inventory scripts.

## Scripts

- `SmartAzure-CostOptimization-Inventory.ps1`: exports common cost optimization signals such as unattached managed disks, old snapshots, unused public IPs, stopped/deallocated VMs, and a subscription summary.
