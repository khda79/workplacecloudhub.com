# SmartM365 Agent Instructions

Before editing this repository, read:

- `RULES.md`
- the nearest folder-specific `RULES.md` for the files being changed

`AGENTS.md` is intentionally short because Codex and other coding agents automatically look for it as project guidance. Human-facing rules live in `RULES.md` files.
