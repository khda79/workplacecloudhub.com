// Smart Workplace Dashboard BETA 3.8.0-beta.1
// Source adapters: fnGetSourceFiles.pq and fnLoadSourceTable.pq.
// Build-SmartWorkplaceDashboard.js embeds these files and the conversion/key helpers
// into model.bim. Edit source files, rebuild, then run the offline regression suites.
// ExpectedTenantKey is required. One tenant root, nonrecursive DATA-LAST.
// Header mismatches, tenant conflicts, partial-source flags and ambiguous identities
// produce explicit refresh errors. Unknown evidence is never stamped as now.
